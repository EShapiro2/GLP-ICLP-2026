# GLP — handover, 2026-09-23

Written by GLP #2 Cowork, and rewritten at the end of its day.  The GLP project holds three papers — the language paper (`/Grassroots/GLP-Spec`, remote `GLP-ICLP-2026`), TGLP (`/Grassroots/TGLP`, remote `Moded-Types`) and IGLP (`/Grassroots/IGLP`, remote `CGLP`) — one session, one inbox, `GLP_inbox.md`.

## What the papers gained today

TGLP, `99332fc`:

- Definition (Instantiation): a map from a callee's parameters to types of the program under which the caller's clause and the callee's clauses are well-typed, and every input path of the instantiated declaration is accepted by some clause.  It replaces the call-site matching rule, which failed on a constructed argument, and coverage is what selects the map, so there is no least-map rule.
- An anonymous variable at a produced position of a clause head is written `_?` and is an output the clause never produces; an anonymous reader at a consumed position stays forbidden.
- Definition (Constant Type) and Proposition (Readers of Constant Types): a reader of a constant type may occur more than once.  It replaces a ground-type sentence of mine that admitted `Stream(Integer)`, and it is Remark "Guards and SRSW" with the groundness coming from the type.
- A guard narrows the head occurrence it tests, by the meet of the two types, and the body is checked under the meet.
- A parametric imported declaration instantiates at the call as a local one does.
- `MutualRef` is a primitive type, with its row in the consistency table; it is not a `Constant`.

GLP-Spec, `0b40849`:

- `MutualRef` in the primitives sentence, and the mutual-reference family typed in the catalogue — the guard, three kernels, three wrappers.
- A new appendix, "An Extension for Stochastic Simulation": the Idle transition, `when_idle(W)`, `Idle ::= idle`, what it leaves untouched in cGLP, and the maGLP caveat that an agent idle now may be busy a moment later.  It is not part of the language.  It is invisible in the proceedings build, `\appendixfalse` at `main-GLP-ICLP-2026.tex:56`.

## What Code has landed, and where

Branches, none merged: `IGLP`, `IGLP-declparams`, `IGLP-ground`, `IGLP-qparams`, `IGLP-linker` (a branch of `IGLP-declparams`), `GSG-declparams`.

- `IGLP-ground`: the constant-type test, the guard meet with condition 3(b) running for the first time, and `MutualRef` at `a6e783f0` — eight rows forward and none back, and `p99/arithmetic`, `lists`, `codes` and `mtrees` now load.  The `mwm` family needed no change: `is_mutual_ref(Ref?)` narrows `_?` to `MutualRef?` before 3(b) compares it.
- `IGLP-linker`, `0a0f20da`: a declaration's parameter list now survives linking — five `ProcDecl` copies dropped it — and six directories move, the generated `responder` module among them.
- `IGLP-qparams`: question parameters in parser, checker and compiler.
- `GSG-declparams`: GSG's fourteen routing lists, and `befriend.glp:4,5` as parametric imports.

## Open, with whom

Mine, tasked and running: the canonical print must carry a declaration's parameter list (`wire/flattening.dart:128,142`, with a round-trip test), IGLP's six unlisted declarations, the scope fix for `ActorOut` at `typed_social_agent.glp:138`, the replacement comment at `programs/self.glp:774--776`, and `execUnifyVoid`'s WRITE arm, which three probes wait on.

Mine, unstarted: ninety-five declaration sites of my own name no parameter list — `programs/tests/` 65, `programs/book/` 30 — listed at `/Users/udi/Grassroots/tmp/iglp_declparams_scan.txt`.  None blocks anything.

Other owners, through Integration Cowork: GSG (`superapp_plays.glp:25`, `currency_txn.glp:36` declaring `Constant?` where `Integer?` belongs, `home.vglp:47`, `budget/bounded.glp:55`, `social/spm/self.glp:33`, and the `Content?`/`Module?` sites with no guard); Currencies (three question-parameter lists that gate suite sections N2–N4, `bonds_v2/boot.glp:67`, `bonds/self.glp:230`, `propose_swap`, `escrow:accepts/6`); Legal (`jurix`) and GFWC (`federation`), each reading a multi-element list type twice, which SRSW* does not license; GLP-Networking-API (the rendezvous port off `valid_attestation/4`, merging with the removal); Integration (the manual's Section 9, and the two files that cite the manual for the paper's rules).

Udi's: CSSN `cssn/boot.glp:125` and P99 `p99/self.glp:45`, both dormant projects with no session, eight directories between them.

svGLP: answered — the idle extension is in the paper and is not implemented.  Their code task is written the day they ask.  Tell them to read their mail.

## How the day went, for the next session

Udi does not want rulings where the math decides.  Three questions I put to him as rulings were decided by the papers, one of them against what I recommended.  Read the definitions before proposing a sentence; where the math does not decide, fix the math rather than rule.

Grep every text against `/Grassroots/docs/claudese.md` before pushing it.  The list grows, and remembering it does not work: `state` of a paper, added on the 21st, went into the new appendix on the 23rd.

Code corrected two of its own measurements today and I withdrew two claims of mine.  Measure before asserting what refuses what, and name the branch a measurement was taken on.

## The next step

Read mail.  Code is running the canonical print and the six lists, and reports on each.
