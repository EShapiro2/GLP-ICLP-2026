\section{Guards and System Predicates}\label{appendix:guards-system}

Guards and system predicates extend GLP programs with access to the GLP runtime state, operating system and hardware capabilities.

\mypara{Predefined types}
The predefined types, available in every GLP program, are \verb|Stream(X)|, \verb|OpenStream(X)|, \verb|DiffList(X)|, \verb|Channel(In, Out)|, \verb|Closed|, \verb|Number|, \verb|Constant|, and the arithmetic-expression type \verb|Exp|; their definitions are in \verb|programs/self.glp| of the public GLP repository. \verb|Integer|, \verb|Real|, \verb|String|, and \verb|Module| are primitive types built into the runtime.

\mypara{Guard predicates}
Guards provide read-only access to the runtime state of GLP computation. A guard appears after the clause head, separated by \verb=|=, and must be satisfied for the clause to be selected. The ``Ground'' column indicates whether success of the guard implies that the argument is ground; such guards permit multiple occurrences of the reader in the clause body (Remark~\ref{rem:guards-srsw}). Built-in guards can be negated: \verb|~G| succeeds if \verb|G| fails and vice versa, while suspension is unchanged; negation applies only to atomic built-in guards, not to defined guards or conjunctions.

\begin{center}
\begin{tabular}{lll}
\textbf{Guard} & \textbf{Signature} & \textbf{Ground} \\
\hline
\verb|integer| & \verb|procedure integer(Integer?).| & yes \\
\verb|number| & \verb|procedure number(Number?).| & yes \\
\verb|string| & \verb|procedure string(String?).| & yes \\
\verb|constant| & \verb|procedure constant(Constant?).| & yes \\
\verb|compound| & \verb|procedure compound(_?).| & no \\
\verb|list| & \verb|procedure list(_?).| & no \\
\verb|ground| & \verb|procedure ground(_?).| & yes \\
\verb|known| & \verb|procedure known(_?).| & no \\
\verb|unknown| & \verb|procedure unknown(_?).| & no \\
\verb|no_readers| & \verb|procedure no_readers(_?).| & no \\
\verb|module| & \verb|procedure module(Module?).| & yes \\
\verb|@<| & \verb|procedure @<(Constant?, Constant?).| & yes \\
\verb|is_mutual_ref| & \verb|procedure is_mutual_ref(_?).| & no \\
\verb|verify| & \verb|procedure verify(_?, Constant?, Constant?).| & yes (all) \\
\verb|otherwise| & (no arguments) & --- \\
\verb|=?=| & \verb|procedure =?=(_?, _?).| & yes (both) \\
\end{tabular}
\end{center}

\verb|otherwise| succeeds if all previous clauses for this procedure failed (or suspended).

\verb|=?=| succeeds if both arguments are ground and equal. For example, \verb|f(a,X?) =?= f(b,Z?)| fails (not suspends) because the ground subterms \verb|a| and \verb|b| already differ. Note that the implementation checks terms left-to-right and does not guarantee early failure detection; \verb|f(X?,a) =?= f(Y?,b)| may suspend rather than fail.

\verb|verify(Term, Sig, Key)| succeeds if and only if \verb|Sig| is a valid signature on the pair of the calling instance's module identity and \verb|Term|, by the person whose public key is \verb|Key|. Including the module identity scopes a signature to the module under which it was made. Signatures are produced by \verb|sign/2| below; the security construction that relies on them is in the Secure GLP companion paper~\cite{keidar2026secure}.

\mypara{Arithmetic comparison guards}
Arithmetic comparison guards evaluate their arguments as arithmetic expressions over \verb|+|, \verb|-|, \verb|*|, \verb|//| (integer division), and \verb|mod|, and compare the results. Success implies both arguments are ground.

\begin{center}
\begin{tabular}{ll}
\textbf{Guard} & \textbf{Signature} \\
\hline
\verb|<| & \verb|procedure <(Exp?, Exp?).| \\
\verb|>| & \verb|procedure >(Exp?, Exp?).| \\
\verb|=<| & \verb|procedure =<(Exp?, Exp?).| \\
\verb|>=| & \verb|procedure >=(Exp?, Exp?).| \\
\verb|=:=| & \verb|procedure =:=(Exp?, Exp?).| \\
\verb|=\=| & \verb|procedure =\=(Exp?, Exp?).| \\
\end{tabular}
\end{center}

\mypara{Time guards}
Time guards provide access to the system clock for scheduling and synchronization:

\begin{center}
\begin{tabular}{ll}
\textbf{Guard} & \textbf{Signature} \\
\hline
\verb|wait| & \verb|procedure wait(Number?).| \\
\verb|wait_until| & \verb|procedure wait_until(Number?).| \\
\end{tabular}
\end{center}
\noindent\verb|wait(D)| suspends for \verb|D| milliseconds then succeeds. \verb|wait_until(T)| suspends until the current time is at or past timestamp~\verb|T| (Unix milliseconds), then succeeds. Neither guard can be negated.

\mypara{Monotonicity and implications}
\verb|ground/1|, \verb|no_readers/1|, and \verb|known/1| are monotonic. \verb|ground(X)| implies both \verb|no_readers(X)| and \verb|known(X)|, which do not imply each other: \verb|no_readers(X)| succeeds but \verb|known(X)| fails for an unassigned writer; \verb|no_readers(f(X?))| suspends but \verb|known(f(X?))| succeeds.

\mypara{Defined guard predicates}
To support abstract data types and cleaner code organization, GLP provides for user-defined guards via unit clauses. A unit clause \verb|p(T1,...,Tn).| defines a guard predicate; the call \verb|p(S1,...,Sn)| in guard position is unfolded to the term matching of \verb|T1| with \verb|S1|, ..., \verb|Tn| with \verb|Sn|. For example, unification is defined by the unit clause \verb|X? = X.|, so the guard \verb|A = B| unfolds to matching both \verb|A| and \verb|B| against the same variable \verb|X|.

The following defined guard predicates are shipped with the runtime:

\begin{center}
\begin{tabular}{ll}
\textbf{Predicate} & \textbf{Unit clause} \\
\hline
\verb|=| & \verb|X? = X.| \\
\verb|new_channel| & \verb|new_channel(ch(Xs?, Ys), ch(Ys?, Xs)).| \\
\verb|send| & \verb+send(X, ch(In, [X?|Out?]), ch(In?, Out)).+ \\
\verb|receive| & \verb+receive(X?, ch([X|In], Out?), ch(In?, Out)).+ \\
\verb|close| & \verb|close(ch([], [])).| \\
\verb|dl_append| & \verb|dl_append(A\B?, B\C?, A?\C).| \\
\verb|dl_to_list| & \verb|dl_to_list(L\[], L?).| \\
\end{tabular}
\end{center}

\mypara{Naming and admission of body kernels}
A body kernel is named by a quoted constant whose name begins with an underscore, as \verb|'_add'| is. Quoting is necessary: an unquoted name beginning with an underscore is an anonymous variable (Remark~\ref{rem:anonymous-variables}). A module declaring \verb|-mode(system)| may define and call procedures whose names are so written; a module that does not may neither define nor call one. The restriction is on names in call position only; a constant beginning with an underscore is unrestricted as data, as a message tag or a member of a type union. The system modules are the root \verb|programs/self.glp| and the modules under \verb|programs/system/|.

\mypara{Body kernel predicates}
\temph{Body kernel predicates} are runtime-implemented primitives that execute in the clause body with two-valued semantics: they either succeed or abort. Body kernels are not directly accessible to user programs. They are invoked only by \emph{system predicates}---predicates defined by GLP clauses that are shipped with the runtime and have privileged access to the body kernel registry. The system predicate's own guards ensure that a body kernel's preconditions are met before invocation.

The following table lists all body kernels:

\begin{center}
{\small
\begin{tabular}{lll}
\textbf{Category} & \textbf{Kernel} & \textbf{Operation} \\
\hline
Arithmetic & \verb|'_add'(Number?, Number?, Number)| & Addition \\
 & \verb|'_sub'(Number?, Number?, Number)| & Subtraction \\
 & \verb|'_mul'(Number?, Number?, Number)| & Multiplication \\
 & \verb|'_div'(Number?, Number?, Number)| & Division (real result) \\
 & \verb|'_idiv'(Integer?, Integer?, Integer)| & Integer division \\
 & \verb|'_mod'(Integer?, Integer?, Integer)| & Modulo \\
 & \verb|'_neg'(Number?, Number)| & Unary negation \\
 & \verb|'_abs'(Number?, Number)| & Absolute value \\
\hline
Random & \verb|'_random'(Integer?, Integer?, Integer, Integer)| & Seeded random integer \\
\hline
Math & \verb|'_sqrt'(Number?, Number)| & Square root \\
 & \verb|'_sin'(Number?, Number)| & Sine \\
 & \verb|'_cos'(Number?, Number)| & Cosine \\
 & \verb|'_tan'(Number?, Number)| & Tangent \\
 & \verb|'_asin'(Number?, Number)| & Arc sine \\
 & \verb|'_acos'(Number?, Number)| & Arc cosine \\
 & \verb|'_atan'(Number?, Number)| & Arc tangent \\
 & \verb|'_exp'(Number?, Number)| & Exponential ($e^x$) \\
 & \verb|'_ln'(Number?, Number)| & Natural logarithm \\
 & \verb|'_log10'(Number?, Number)| & Base-10 logarithm \\
 & \verb|'_pow'(Number?, Number?, Number)| & Power ($x^y$) \\
\hline
Conversion & \verb|'_integer'(Number?, Integer)| & Convert to integer \\
 & \verb|'_real'(Number?, Number)| & Convert to float \\
 & \verb|'_round'(Number?, Integer)| & Round to nearest integer \\
 & \verb|'_floor'(Number?, Integer)| & Floor \\
 & \verb|'_ceil'(Number?, Integer)| & Ceiling \\
\hline
Structure & \verb|'_list_to_tuple'(Stream(_)?, _)| & List to compound term \\
 & \verb|'_tuple_to_list'(_?, Stream(_))| & Compound term to list \\
\hline
Copy & \verb|'_copy'(_?, _)| & Bind the second argument to the first's value \\
\hline
Time & \verb|'_now'(Integer)| & Current Unix timestamp (ms) \\
\hline
I/O & \verb|'_output'(_?)| & Send term to output callback \\
\hline
MWM & \verb|'_allocate_mutual_reference'(_, _)| & Create mutual reference for O(1) append \\
 & \verb|'_stream_append'(_?, _?, _)| & Append value via mutual reference \\
 & \verb|'_close_mutual_reference'(_?)| & Close stream (bind tail to \verb|[]|) \\
\hline
Network & \verb|'_send'(_?, _?, _?)| & Send madGLP message to remote agent \\
 & \verb|'_authorise_link'(_?, _?)| & Release or drop a reported held link \\
 & \verb|'_peer_address'(_?, _)| & Address at which the layer observes a peer \\
 & \verb|'_punch_udp'(_?)| & Open a path to an address \\
 & \verb|'_place_declare'(_?, _?, _)| & Declare a place; stream its crossing and observability events \\
 & \verb|'_place_remove'(_?)| & End a place declaration \\
 & \verb|'_trust_declare'(_?, _?)| & Set a proximity medium's cold-call trust level \\
\hline
Signature & \verb|'_sign'(_?, _)| & Sign a term under the calling module \\
\hline
Modules & \verb|'_self_module'(Module)| & The caller's own module \\
 & \verb|'_run'(_?, Module?)| & Activate module and post goal to it \\
\end{tabular}
}
\end{center}

\mypara{System predicates}
System predicates are predicates defined by GLP clauses whose bodies may invoke body kernel predicates. They provide safe, user-accessible interfaces to runtime services:

\begin{center}
\begin{tabular}{ll}
\textbf{Predicate} & \textbf{Description} \\
\hline
\verb|=..| & Term composition and decomposition (bidirectional) \\
\verb|now/1| & Current Unix timestamp (milliseconds) \\
\verb|:=/2| & Arithmetic evaluation and assignment \\
\verb|random/4| & Seeded random integer generation \\
\verb|send_to_person/1| & Emit each term of a stream to the person \\
\verb|self_module/1| & The caller's own module, as a \verb|Module| value \\
\verb|run/2| & Activate a module value and post a goal to it \\
\verb|run/3| & As \verb|run/2|, under a required type identity \\
\verb|find_type/2| & The type identity of a procedure's declaration \\
\verb|sign/2| & Signature by the person, under the calling module \\
\verb|authorise_link/2| & Release or drop a reported held link \\
\verb|peer_address/2| & Address at which the layer observes a peer \\
\verb|punch_udp/1| & Open a path to an address \\
\verb|place_declare/3| & Declare a place; stream its crossing and observability events \\
\verb|place_remove/1| & End a place declaration \\
\verb|trust_declare/2| & Declare a proximity medium's cold-call trust level \\
\verb|allocate_mutual_reference/2| & Create a mutual reference and the stream it builds \\
\verb|stream_append/3| & Append a value to that stream \\
\verb|close_mutual_reference/1| & End that stream \\
\end{tabular}
\end{center}

\mypara{Term composition and decomposition (\texttt{=..})}
The \verb|=..| predicate is bidirectional: when the right-hand side is a list, it composes a compound term; when the left-hand side is a compound term, it decomposes it into a list. The reader/writer positions of the arguments determine the direction. For example, \verb|T =.. [foo, a, b]?| constructs \verb|T = foo(a, b)|, and \verb|foo(a, b) =.. L| produces \verb|L = [foo, a, b]|.

\mypara{Clock access (\texttt{now/1})}
Binds the argument to the current Unix timestamp in milliseconds since the epoch.

\mypara{Arithmetic evaluation and assignment (\texttt{:=})}
Arithmetic assignment is defined recursively: each clause pattern-matches on an arithmetic operator, guards verify that operands are of the required type, and the body invokes the corresponding body kernel. An \verb|otherwise| clause handles the case where operands are themselves expressions, evaluating them recursively before retrying.

\mypara{Seeded random numbers (\texttt{random/4})}
\verb|random(Seed, N, Value, NextSeed)| binds \verb|Value| to a pseudo-random integer with $1 \leq \mathtt{Value} \leq \mathtt{N}$, and \verb|NextSeed| to the seed of the next draw, given a ground seed \verb|Seed| and a ground bound $\mathtt{N} \geq 1$. It is a body goal, not a guard; the clause is guarded by \verb|integer(Seed?), integer(N?), N? >= 1| and invokes the \verb|'_random'| body kernel. \verb|NextSeed| is determined by \verb|Seed| alone; \verb|Value| by \verb|Seed| and \verb|N|. The seed stream is therefore independent of the bound requested, and a computation that threads \verb|NextSeed| from each draw into the next reproduces exactly from its initial seed.

\mypara{Output to the person (\texttt{send\_to\_person/1})}
\verb|send_to_person(Stream)| emits each element of \verb|Stream| to the person operating the agent, in order, and ends when the stream ends. Its clause is guarded by \verb|ground| on the element and invokes the \verb|'_output'| body kernel. A module reaches the person through this predicate; naming \verb|'_output'| in call position is admitted only to a system module.

\mypara{Module as a value (\texttt{self\_module/1})}
\verb|self_module(Module)| binds its argument to the caller's own module, a value of the primitive type \verb|Module|.

\mypara{Dynamic activation (\texttt{run/2}, \texttt{run/3}, \texttt{find\_type/2})}
\verb|run(Goal, Module)| activates the module value \verb|Module| and posts \verb|Goal| to it; the predicate of \verb|Goal| must be exported by \verb|Module|, else an error. The readers and writers of \verb|Goal| connect the caller and the activated module. \verb|run(Goal, Type, Module)| activates \verb|Module| and posts \verb|Goal| to it where \verb|Type| equals the type identity that \verb|Module|'s exports record for \verb|Goal|'s predicate, and errs otherwise. \verb|find_type(P/N, Type)| assigns \verb|Type| the type identity of the declaration of \verb|P/N| in the caller's scope, and \verb|error| where \verb|P/N| is not declared there. Type identity is defined in the Typed GLP companion paper~\cite{shapiro2026types}, which also gives its construction and proves the compatibility it rests on. \verb|run/3| and \verb|find_type/2| are not yet implemented.

\mypara{Signing (\texttt{sign/2})}
\verb|sign(Term, Sig)| requires \verb|Term| ground and binds \verb|Sig| to the signature of the person operating the agent on the pair of the calling instance's module identity and \verb|Term|, as an opaque constant. The guard \verb|verify| checks it.

\mypara{Held links (\texttt{authorise\_link/2})}
\verb|authorise_link(L, A)| answers a link that the runtime holds and reports because its other end is at an agent with which the name was not exchanged. \verb|L| is the ground term naming the reported link and \verb|A| is \verb|authorise| or \verb|refuse|: on \verb|authorise| the runtime releases the held message or assignment, on \verb|refuse| it drops the link. The clause is guarded by \verb|known(A?)| and invokes the \verb|'_authorise_link'| body kernel. Held links are specified in the companion implementation paper~\cite{shapiro2026implementing}.

\mypara{Networking seam (\texttt{peer\_address/2}, \texttt{punch\_udp/1}, \texttt{place\_declare/3}, \texttt{place\_remove/1})}
\verb|peer_address(P, A)| assigns \verb|A| the address at which the networking layer observes peer \verb|P|. \verb|punch_udp(A)| opens a path to address \verb|A| and assigns nothing. \verb|place_declare(P, R, E)| declares a place \verb|P| of radius \verb|R|, centred on the declaring agent's location at the time of the call, and assigns \verb|E| a stream of that agent's own \verb|entered| and \verb|exited| events for the place, interleaved with \verb|unobservable| where the layer has stopped reporting crossings and \verb|observable| where it resumes; between the two no crossing is seen and none is delivered late. A further declaration of the same place supersedes the earlier one and closes its stream. \verb|place_remove(P)| ends the declaration of \verb|P|, and does nothing where \verb|P| is not declared. Each clause is guarded by \verb|ground| on the arguments it consumes. The layer behind these four is specified in the companion implementation paper~\cite{shapiro2026implementing}.

\mypara{Proximity trust (\texttt{trust\_declare/2})}
A \emph{proximity medium} carries a \emph{cold-call trust level}, which decides whether the agent answers a nearby device it does not already know. \verb|trust_declare(Medium, Level)| declares that level for one medium: \verb|Medium| is \verb|ble| or \verb|lan|, and \verb|Level| is \verb|open| or \verb|closed|. Under \verb|open| the agent contacts and answers unrecognised presences on that medium, revealing its public key to them; under \verb|closed| it ignores them, and its presence serves only to let peers who already know it recognise it. Each medium carries its own level, so an agent may answer strangers over one and ignore them over the other. One declaration stands per medium and a later one supersedes it. The clause is guarded by \verb|ground| on both arguments. The level is the program's to set and not the networking layer's to choose; the layer beneath it is specified in the companion implementation paper~\cite{shapiro2026implementing}.

\mypara{Mutual references (\texttt{allocate\_mutual\_reference/2}, \texttt{stream\_append/3}, \texttt{close\_mutual\_reference/1})}
A \emph{mutual reference} is a handle on the tail of a stream, giving the stream a constant-time append. \verb|allocate_mutual_reference(Ref, Out)| binds \verb|Ref| to a fresh mutual reference and \verb|Out| to the stream it builds. \verb|stream_append(Value, RefIn, RefOut)| appends \verb|Value| to that stream and binds \verb|RefOut| to the reference for the next append. \verb|close_mutual_reference(Ref)| ends the stream, binding its tail to \verb|[]|. The clauses of the latter two are guarded by \verb|is_mutual_ref| on the reference they consume.

The complete definitions of \verb|:=|, \verb|now|, \verb|=..|, \verb|random|, \verb|send_to_person|, \verb|self_module|, \verb|run|, \verb|sign|, \verb|authorise_link|, \verb|allocate_mutual_reference|, \verb|stream_append|, and \verb|close_mutual_reference| are in \verb|programs/self.glp| of the public GLP repository.

